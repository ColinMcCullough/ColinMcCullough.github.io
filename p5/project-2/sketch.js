function setup() {
  var canvas = createCanvas(180, 180);
 
  // Move the canvas so it’s inside our <div id="sketch-holder">.
  canvas.parent('sketch-holder');

  background(100, 0, 200);
}
  
function draw() {
    background(204);
    ellipse(50, 50, 80, 80);
    }
  


